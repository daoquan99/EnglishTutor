# Speaking Session Workflow

## Overview

User starts a speaking session, submits turns, receives AI correction, and sees session results.

## Flow

```text
Start session
→ submit text/audio turn
→ wait for AI correction
→ show corrected sentence and scores
→ complete session
→ show summary
```
